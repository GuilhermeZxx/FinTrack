apiVersion: v1
kind: Service
metadata:
  name: front-fintrack
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
   - protocol: TCP
     port: 5173
     targetPort: 5173
     nodePort: 30000